spring:
  application:
    name: gateway  # 服务名
  profiles:
    active: dev
  cloud:
    nacos:
      server-addr: 192.168.31.188:8848 # nacos地址
      config:
        file-extension: yaml
        shared-configs:
          - data-id: shared-log.yaml
